# elemanai
آموزش و مشاوره کنکور با هوش مصنوعی و تحلیل

## اجرا

1. نصب وابستگی‌ها:

```bash
pip install -r requirements.txt
```

2. اجرای برنامه:

```bash
streamlit run streamlitai2.py --server.headless true --server.port 8501
```

سپس در مرورگر آدرس زیر را باز کنید:

- `http://localhost:8501`
