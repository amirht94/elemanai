# بررسی فنی پروژه elemanai

## مزایا

1. **پوشش کامل جریان کار در یک اپ واحد**
   - دریافت داده، آموزش مدل، تحلیل، بهینه‌سازی برنامه مطالعه و خروجی CSV همگی در یک رابط Streamlit پیاده‌سازی شده‌اند.

2. **Fallback برای دیتابیس**
   - ابتدا PostgreSQL تلاش می‌شود و در صورت خطا، SQLite استفاده می‌شود که برای دموی سریع مفید است.

3. **استفاده از مدل ML نسبتاً قوی**
   - استفاده از `XGBClassifier` به‌همراه feature engineering ساده (`lesson_code`) برای مسئله تشخیص ضعف قابل قبول است.

4. **بهینه‌سازی برنامه‌مطالعه**
   - تخصیص زمان مطالعه بر اساس ضعف و سختی با الگوریتم ژنتیک، ایده خوبی برای تصمیم‌یار است.

## معایب

1. **ریسک امنیتی بسیار بالا (Credentials هاردکد شده)**
   - اطلاعات اتصال دیتابیس داخل کد قرار گرفته که نباید در repo باشد.

2. **اختلاط SQL placeholderها بین SQLite و PostgreSQL**
   - در کد از placeholder مدل SQLite (`?`) استفاده شده ولی در اتصال PostgreSQL باید `%s` استفاده شود؛ این می‌تواند در حالت PostgreSQL خطا ایجاد کند.

3. **مدیریت خطاهای کلی و مبهم**
   - استفاده از `except:` بدون نوع خطا، عیب‌یابی را سخت می‌کند و خطاهای غیرمنتظره را پنهان می‌سازد.

4. **وابستگی state به فایل‌های محلی مدل**
   - فایل‌های `label_encoder.pkl` و `weakness_model.pkl` و `feature_names.pkl` روی دیسک محلی ذخیره می‌شوند؛ در محیط چندکاربره یا Cloud scale مناسب نیست.

5. **عدم جداسازی لایه‌ها (UI/DB/ML)**
   - کل منطق در یک فایل تک‌اسکریپت قرار گرفته؛ تست‌پذیری و نگهداشت را سخت می‌کند.

6. **امکان شکست در Label Encoding روی کلاس‌های جدید**
   - با ورود درس جدید نسبت به آموزش قبلی، تبدیل `LabelEncoder` ممکن است خطا بدهد یا رفتار ناسازگار ایجاد کند.

7. **غیربهینه بودن آموزش و ارزیابی**
   - روی dataset کوچک، `train_test_split` بدون random_state و بدون stratify ممکن است گزارش ناپایدار بدهد.

## پیشنهادهای اولویت‌دار

1. انتقال همه secretها به متغیر محیطی یا secret manager.
2. یکسان‌سازی لایه داده با SQLAlchemy یا جداسازی query برای SQLite/PostgreSQL.
3. ماژولار کردن کد به حداقل سه فایل: `ui.py`, `ml.py`, `db.py`.
4. افزودن `random_state` و مدیریت داده‌های کم‌حجم/imbalanced در train/test.
5. افزودن logging ساختاریافته و خطاهای نوع‌دار.
